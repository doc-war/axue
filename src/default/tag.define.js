import { axueDefaultTagName } from "../frame/tagName";
export const defaultTagName = axueDefaultTagName ?? {} //默认标签名，原样导出

// //不排除未来是否需要封装其他功能，这里纯封装

// // //统一输出默认标签名的枚举值
// // export const defaultTagName= globalThis.axueDefaultTagName??{}
// // Object.freeze(defaultTagName);   //冻结

